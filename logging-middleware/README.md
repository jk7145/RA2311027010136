# Logging Middleware

This directory contains the logging middleware for the notification system.

## Usage
Import and use in your Express app:

```javascript
const logger = require('./logging-middleware');
app.use(logger);
```

## Features
- Request logging
- Response logging
- Error tracking
- Performance monitoring
